# Figma Reference Copy

Raw React components copied from `LeadVirt-React-design-only/src/app`.

This folder is intentionally outside `apps/web/src` so the Next app does not compile the design-only Vite/shadcn export and its extra dependencies directly. Copy selected components into `src` when wiring them to the production app and API-backed data.
